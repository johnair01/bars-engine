# Tasks: Inner Garden — Multi-Channel Blocker & Route-Hand Capacity

- [ ] **T1** `gate-confrontation.ts`: add `ChannelThread`, `BlockerSignature = ChannelThread[]`;
  import `EmotionChannel`/`SatisfactionSpirit` from `emotional-alchemy`.
- [ ] **T2** `threadRouteHand(thread)` → `{ required, optional }` (metabolize-to-neutral vs
      transcend-to-spirit); `requiredRouteHand(threads)` = union of required. Altitude-preserving.
- [ ] **T3** Rewrite `resolveBlocker` → per-thread; a thread resolves at **neutral** (required
      capacity owned); `resolved` iff all threads reach ≥ neutral; report `reachedNeutral`/
      `reachedSpirit` per thread. Remove the lossy single-vector collapse.
- [ ] **T3b** `BlockerOrigin` + `Blocker { origin, threads }`; `inferBlockerForStagnantSeed`
      (null within window, `origin:'inferred'` past it). Blockers are optional — no gate without one.
- [ ] **T4** `demonstration.ts`: demonstrate per thread; blocker completes when all threads pass.
- [ ] **T5** `move-crafting.ts`: `buildCraftSkeleton` per thread; keys altitude-preserving.
- [ ] **T6** `decomposeBlockerFromText(text)` — deterministic keyword scaffold + typed AI seam.
- [ ] **T7** Rewrite the four `__tests__` suites for multi-channel.
- [ ] **T8** Add the **over-grant regression** (partially-owned multi-thread blocker → not resolved).
- [ ] **T9** Add the **canonical hard-email** case (fear→wonder + anger→triumph; clears when both reach neutral).
- [ ] **T9b** Tests for **neutral-suffices** (metabolize-only resolves a thread), **optionality**
      (no-blocker seed is directly actionable), and **inferred blocker** on a stagnant seed.
- [ ] **T10** `npm run test:inner-garden-ontology` green; update `docs/…-ontology-master.md` §7/§11
      to the multi-channel model and drop the retired-framing muddiness the hostile review flagged.
</content>
