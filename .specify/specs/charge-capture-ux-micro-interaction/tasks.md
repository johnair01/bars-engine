# Tasks: Charge Capture UX Micro-Interaction v0 (GD)

- [x] **T1** Emotion selector moved above text input; 5-column grid with large tap targets.
- [x] **T2** Text input autofocuses after emotion tap (`useEffect` on `emotion` state) and on first render when no emotion selected.
- [x] **T3** Satisfaction + intensity collapsed into "▼ Add intensity & satisfaction" toggle below submit.
- [x] **T4** Post-capture triage: `PostCaptureActions` component with Reflect (321), Explore (quest suggestions), Act (NOW dashboard). Compact variant for multi-capture session list.
- [x] **T5** `npm run check` passes (0 errors).
