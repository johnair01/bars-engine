# Plan: BBR P3

1. `ResidencyEventsCallout` — `src/components/campaign/ResidencyEventsCallout.tsx`
2. Wire `CampaignHubView` for `campaignRef === 'bruised-banana'`
3. `CampaignModal` — Apr 4 / Apr 5 links when `campaignRef === 'bruised-banana'`
4. `OrientationCompass` — optional `residencyEventsHref`; `page.tsx` passes when BB context
5. `NavBar` — Events → `/event`
6. Verify: `npm run check`
