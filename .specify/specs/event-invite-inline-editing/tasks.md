# Tasks: Event invite inline editing

- [x] Spec + plan (this folder)
- [x] `updateEventInviteBarContent` + revalidate paths
- [x] `EventInviteBarContentEditor` component
- [x] Invite page: `canEdit` + editor
- [x] Vault: query fields + editor per BAR row
- [x] `npm run check` && `npm run build` (tsc verified; full check may take several minutes)
