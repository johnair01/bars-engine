# Tasks: Composer CYOA → CyoaBuild

- [ ] Choose persistence strategy; add Prisma field or table if needed (`db:sync`).
- [ ] `POST` / server action: **commit** `CyoaBuild` from composer terminal (validate Zod).
- [ ] `GET` / loader: **read** build for player + campaign before spoke.
- [ ] Author or seed **composer** adventure (minimal passage graph).
- [ ] Wire hub CTA → composer → spoke.
- [ ] Update parent spec §4 follow-ups when slice lands.
