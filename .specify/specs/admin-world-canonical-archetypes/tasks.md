# Tasks: Admin World — Canonical Archetypes Only

- [x] Create `src/lib/canonical-archetypes.ts` with `CANONICAL_ARCHETYPE_NAMES`
- [x] Update `getAdminWorldData` to filter playbooks by canonical names
- [ ] Run `npm run build` and `npm run check`
- [ ] Verify Admin World page shows 8 archetypes locally (or document manual check on deploy)
