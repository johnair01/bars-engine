# Backlog prompt — PDC (Avatar Paper Doll Coherence)

**Backlog ID:** 1.42 · **Spec:** [.specify/specs/avatar-paper-doll-coherence/spec.md](../specs/avatar-paper-doll-coherence/spec.md)

## Intent

Portrait avatars stack layer PNGs without a shared anchor; figures look incoherent. Implement the **plan phases**: art contract → offset registry (optional) → `Avatar` rendering + CI validation. Coordinate with **ARDS** Portrait register so rules are not duplicated.

## Paste for agents

Implement per `.specify/specs/avatar-paper-doll-coherence/` — follow `tasks.md` in order. Do not conflate with walkable map sprites (`getWalkableSpriteUrl`); those are precomposed sheets.
