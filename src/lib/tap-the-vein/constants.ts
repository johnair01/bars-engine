/** Tap the Vein shared constants. Kept out of the "use server" actions file —
 *  a server-action module may only export async functions. */

/** Max tasks a player commits per Tap the Vein day (design: "name up to five"). */
export const MAX_TASKS_PER_DAY = 5
