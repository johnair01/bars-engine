# Cursor Rules for BARs Engine

## Environment: OPENAI_API_KEY

Agent workflows (Sage consult, strand, sage:brief, bars-agents MCP) require `OPENAI_API_KEY` in the backend environment. Without it, agents use deterministic fallbacks — no real synthesis. Add to `.env.local`; run `npm run smoke` to verify. See `docs/AGENT_WORKFLOWS.md` troubleshooting.

**bars-agents MCP:** Before treating MCP as “unavailable” or skipping `sage_consult`, run `npm run verify:bars-agents-mcp` and confirm **Cursor → Settings → MCP → bars-agents** is on (reload window if needed). Do not skip this when the user requires MCP-backed synthesis. See `docs/AGENT_WORKFLOWS.md` § MCP availability. If MCP shows **Error**, see **`docs/CURSOR_MCP_TROUBLESHOOTING.md`** (what to paste from Show output; PATH/`uv` fixes).

## Database Synchronization

When you modify the file `prisma/schema.prisma`, you MUST automatically run:

```bash
npm run db:sync
```

This ensures:
1. The database schema is updated (via `prisma db push`)
2. The Prisma Client is regenerated with new types
3. Other developers on different machines receive updates seamlessly

Do not wait for the user to ask; execute this as a post-modification step.

---

## See Also

- **`.cursor/rules/game-master-agents.mdc`** — Game Master faces, Cursor subagent mapping, when to use bars-agents
- **`.cursor/rules/spec-kit-plans.mdc`** — Plans must produce spec kit output (spec, plan, tasks in .specify/specs/)
- **`.cursor/rules/fail-fix-workflow.mdc`** — Verify builds and tests before moving on (frontend, backend, database)
- **`.cursor/rules/branch-context-nn-edits.mdc`** — Full context for this branch, cross-platform notes, quick reference
- **`docs/SYNTHETIC_VS_REAL.md`** — How to switch between local and Vercel databases
- **`Makefile`** — Run `make help` to see all available development commands
